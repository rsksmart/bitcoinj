buildscript {
    repositories {
        mavenCentral()
    }

    dependencies {
        classpath 'com.google.protobuf:protobuf-gradle-plugin:0.8.6'
    }
}

allprojects {
    repositories {
        mavenCentral()
    }

    group = 'org.bitcoinj'
}

subprojects {
    tasks.withType(Javadoc).all { enabled = false }
}
